use crate::game701::common::Id;

use super::Unit;

impl<'a> Unit<'a> {
    // pub fn scan_touch_stand_enemy(&self) -> Vec<Id> {
    //     let team = self.team();
    //     for id in 0..self.board.len() {
    //         if (
    //             id != self.id() // not self
    //             && team != self.board.unit(id).team() // enemy
    //             && 
    //         ) {

    //         }
    //     }
    //     todo!()
    // }
}